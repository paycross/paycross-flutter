// Autogenerated from Pigeon (v27.3.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.paycross.flutter.generated

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object PayCrossApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * The backend the native SDK talks to.
 *
 * Two cases only. iOS additionally has a `.custom(baseURL:)`; Android has no
 * equivalent, and its Retrofit client derives the base URL from the enum. A
 * `custom` case here would mean the same Dart call did different things on
 * each platform.
 */
enum class PcEnvironment(val raw: Int) {
  SANDBOX(0),
  PRODUCTION(1);

  companion object {
    fun ofRaw(raw: Int): PcEnvironment? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Prefills the native card form for manual test runs.
 *
 * CARRIES A PAN AND A CVV, and is the only card data that ever crosses this
 * channel. It does so because the native form is one the plugin cannot reach
 * any other way. Both native SDKs already drop it outside sandbox; the Dart
 * facade throws if it is supplied with a production environment, and the
 * plugin drops it again before calling in.
 *
 * Nothing about a real shopper's card crosses: CardData, CardFormState and
 * BrowserInfo are absent from this file by design.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PcTestCardPrefill (
  val cardholderName: String,
  val pan: String,
  /** "MM". */
  val expireMonth: String,
  /** Four digits, e.g. "2028". Both native forms take the last two. */
  val expireYear: String,
  val cvv: String,
  val saveCard: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PcTestCardPrefill {
      val cardholderName = pigeonVar_list[0] as String
      val pan = pigeonVar_list[1] as String
      val expireMonth = pigeonVar_list[2] as String
      val expireYear = pigeonVar_list[3] as String
      val cvv = pigeonVar_list[4] as String
      val saveCard = pigeonVar_list[5] as Boolean
      return PcTestCardPrefill(cardholderName, pan, expireMonth, expireYear, cvv, saveCard)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      cardholderName,
      pan,
      expireMonth,
      expireYear,
      cvv,
      saveCard,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PcTestCardPrefill
    return PayCrossApiPigeonUtils.deepEquals(this.cardholderName, other.cardholderName) && PayCrossApiPigeonUtils.deepEquals(this.pan, other.pan) && PayCrossApiPigeonUtils.deepEquals(this.expireMonth, other.expireMonth) && PayCrossApiPigeonUtils.deepEquals(this.expireYear, other.expireYear) && PayCrossApiPigeonUtils.deepEquals(this.cvv, other.cvv) && PayCrossApiPigeonUtils.deepEquals(this.saveCard, other.saveCard)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.cardholderName)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.pan)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.expireMonth)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.expireYear)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.cvv)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.saveCard)
    return result
  }
  override fun toString(): String {
    return "PcTestCardPrefill(cardholderName=$cardholderName, pan=$pan, expireMonth=$expireMonth, expireYear=$expireYear, cvv=$cvv, saveCard=$saveCard)"
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class PcConfiguration (
  val environment: PcEnvironment,
  /**
   * Packed ARGB, sent unsigned. Kotlin receives Long? and calls toInt(), whose
   * low-32-bit truncation is exactly the signed @ColorInt Android wants.
   *
   * Null means "platform default" and must stay a real null: 0x00000000 is
   * transparent black, a legal colour rather than a sentinel.
   *
   * Android only in v1 — the iOS SDK has no brand-colour hook, and its only
   * colour source is the host app's window tint, which the plugin must not set
   * because tintColor inherits down the hierarchy and would repaint the
   * merchant's entire app. iOS accepts and ignores this.
   */
  val brandColorArgb: Long? = null,
  val testCardPrefill: PcTestCardPrefill? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PcConfiguration {
      val environment = pigeonVar_list[0] as PcEnvironment
      val brandColorArgb = pigeonVar_list[1] as Long?
      val testCardPrefill = pigeonVar_list[2] as PcTestCardPrefill?
      return PcConfiguration(environment, brandColorArgb, testCardPrefill)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      environment,
      brandColorArgb,
      testCardPrefill,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PcConfiguration
    return PayCrossApiPigeonUtils.deepEquals(this.environment, other.environment) && PayCrossApiPigeonUtils.deepEquals(this.brandColorArgb, other.brandColorArgb) && PayCrossApiPigeonUtils.deepEquals(this.testCardPrefill, other.testCardPrefill)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.environment)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.brandColorArgb)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.testCardPrefill)
    return result
  }
  override fun toString(): String {
    return "PcConfiguration(environment=$environment, brandColorArgb=$brandColorArgb, testCardPrefill=$testCardPrefill)"
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class PcVersionInfo (
  val pluginVersion: String,
  /**
   * Nullable because the Android SDK exposes no version constant. The null
   * makes that gap visible rather than inventing a string.
   */
  val nativeSdkVersion: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PcVersionInfo {
      val pluginVersion = pigeonVar_list[0] as String
      val nativeSdkVersion = pigeonVar_list[1] as String?
      return PcVersionInfo(pluginVersion, nativeSdkVersion)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      pluginVersion,
      nativeSdkVersion,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PcVersionInfo
    return PayCrossApiPigeonUtils.deepEquals(this.pluginVersion, other.pluginVersion) && PayCrossApiPigeonUtils.deepEquals(this.nativeSdkVersion, other.nativeSdkVersion)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.pluginVersion)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.nativeSdkVersion)
    return result
  }
  override fun toString(): String {
    return "PcVersionInfo(pluginVersion=$pluginVersion, nativeSdkVersion=$nativeSdkVersion)"
  }
}

/**
 * A monetary amount in minor units.
 *
 * Pigeon `int` is Kotlin `Long` and Swift `Int64`. Never a double: a double
 * amount is the most expensive bug class in payments.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PcAmount (
  val minorUnits: Long,
  /**
   * ISO 4217. May be the empty string when the session was already complete
   * and the server returned no transaction reference.
   */
  val currencyCode: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PcAmount {
      val minorUnits = pigeonVar_list[0] as Long
      val currencyCode = pigeonVar_list[1] as String
      return PcAmount(minorUnits, currencyCode)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      minorUnits,
      currencyCode,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PcAmount
    return PayCrossApiPigeonUtils.deepEquals(this.minorUnits, other.minorUnits) && PayCrossApiPigeonUtils.deepEquals(this.currencyCode, other.currencyCode)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.minorUnits)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.currencyCode)
    return result
  }
  override fun toString(): String {
    return "PcAmount(minorUnits=$minorUnits, currencyCode=$currencyCode)"
  }
}

/**
 * How a payment ended.
 *
 * An empty sealed parent with concrete subclasses is the one inheritance shape
 * Pigeon supports, and only in the Kotlin, Swift and Dart generators — which
 * are exactly the three targets here. That is what lets the exhaustiveness the
 * native SDKs were built around survive the crossing.
 *
 * Generated class from Pigeon that represents data sent in messages.
 * This class should not be extended by any user class outside of the generated file.
 */
sealed class PcPaymentResult 
/** Generated class from Pigeon that represents data sent in messages. */
data class PcSuccess (
  /**
   * Empty in the edge case where the session was already complete and the
   * server returned no transaction reference. Not null — both native SDKs
   * model this as a present-but-empty string.
   */
  val transactionId: String,
  /** "success", "authorized", … */
  val status: String,
  val amount: PcAmount
) : PcPaymentResult()
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PcSuccess {
      val transactionId = pigeonVar_list[0] as String
      val status = pigeonVar_list[1] as String
      val amount = pigeonVar_list[2] as PcAmount
      return PcSuccess(transactionId, status, amount)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      transactionId,
      status,
      amount,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PcSuccess
    return PayCrossApiPigeonUtils.deepEquals(this.transactionId, other.transactionId) && PayCrossApiPigeonUtils.deepEquals(this.status, other.status) && PayCrossApiPigeonUtils.deepEquals(this.amount, other.amount)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.transactionId)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.status)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.amount)
    return result
  }
  override fun toString(): String {
    return "PcSuccess(transactionId=$transactionId, status=$status, amount=$amount)"
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class PcFailure (
  /** Null for failures that happen before a transaction exists. */
  val transactionId: String? = null,
  /**
   * The RAW server token, not a parsed enum.
   *
   * Pigeon cannot express an enum with an associated value, so a closed enum
   * here would destroy any value the server adds later — exactly the case
   * iOS's `.unrecognized(String)` exists to preserve. Dart re-parses this with
   * the same rules both native SDKs use, so a new token degrades to
   * "unrecognised, not retryable" instead of being silently rewritten.
   *
   * Empty string means the server sent nothing, which both SDKs read as retry.
   */
  val recovery: String
) : PcPaymentResult()
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PcFailure {
      val transactionId = pigeonVar_list[0] as String?
      val recovery = pigeonVar_list[1] as String
      return PcFailure(transactionId, recovery)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      transactionId,
      recovery,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PcFailure
    return PayCrossApiPigeonUtils.deepEquals(this.transactionId, other.transactionId) && PayCrossApiPigeonUtils.deepEquals(this.recovery, other.recovery)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.transactionId)
    result = 31 * result + PayCrossApiPigeonUtils.deepHash(this.recovery)
    return result
  }
  override fun toString(): String {
    return "PcFailure(transactionId=$transactionId, recovery=$recovery)"
  }
}

/** Generated class from Pigeon that represents data sent in messages. */
class PcCancelled  : PcPaymentResult() {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PcCancelled {
      return PcCancelled()
    }
  }
  fun toList(): List<Any?> {
    return listOf(
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    return true
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    return result
  }
  override fun toString(): String {
    return "PcCancelled()"
  }
}
private open class PayCrossApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          PcEnvironment.ofRaw(it.toInt())
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PcTestCardPrefill.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PcConfiguration.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PcVersionInfo.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PcAmount.fromList(it)
        }
      }
      134.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PcSuccess.fromList(it)
        }
      }
      135.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PcFailure.fromList(it)
        }
      }
      136.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PcCancelled.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is PcEnvironment -> {
        stream.write(129)
        writeValue(stream, value.raw.toLong())
      }
      is PcTestCardPrefill -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is PcConfiguration -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is PcVersionInfo -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is PcAmount -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      is PcSuccess -> {
        stream.write(134)
        writeValue(stream, value.toList())
      }
      is PcFailure -> {
        stream.write(135)
        writeValue(stream, value.toList())
      }
      is PcCancelled -> {
        stream.write(136)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface PayCrossHostApi {
  /**
   * Applies configuration to the native SDK.
   *
   * The plugin caches this and re-applies it lazily inside presentPayment.
   * Without that, a hot restart or a second engine in an add-to-app host
   * loses the Dart-side flag while the native process keeps running, and iOS
   * hits assertionFailure — a hard trap in a debug build.
   */
  fun configure(configuration: PcConfiguration)
  fun versionInfo(): PcVersionInfo
  /**
   * Presents the native payment UI, resolving when the payment reaches a
   * terminal state or the shopper cancels.
   *
   * @async because this can take minutes: the poll deadline is 480s on both
   * platforms. Callers must not wrap the returned Future in Future.timeout —
   * a shorter timeout orphans a live payment while the native SDK keeps
   * polling and may still charge the card.
   *
   * Declines are NOT errors; they arrive as PcFailure. An error from this
   * method always means an integration mistake.
   */
  fun presentPayment(sessionToken: String, callback: (Result<PcPaymentResult>) -> Unit)

  companion object {
    /** The codec used by PayCrossHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      PayCrossApiPigeonCodec()
    }
    /** Sets up an instance of `PayCrossHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: PayCrossHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.paycross_flutter.PayCrossHostApi.configure$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val configurationArg = args[0] as PcConfiguration
            val wrapped: List<Any?> = try {
              api.configure(configurationArg)
              listOf(null)
            } catch (exception: Throwable) {
              PayCrossApiPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.paycross_flutter.PayCrossHostApi.versionInfo$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.versionInfo())
            } catch (exception: Throwable) {
              PayCrossApiPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.paycross_flutter.PayCrossHostApi.presentPayment$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val sessionTokenArg = args[0] as String
            api.presentPayment(sessionTokenArg) { result: Result<PcPaymentResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(PayCrossApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(PayCrossApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
