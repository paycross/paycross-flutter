# Deliberately empty. The rules this build needs come from the libraries
# themselves -- the PayCross SDK's consumer-rules.pro and Flutter's embedding
# -- and a merchant should not have to add any. A rule appearing here would
# mean the SDK is not shipping one it should, which is a finding to file
# rather than a line to add.
