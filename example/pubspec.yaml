name: paycross_demo
description: "PayCross Demo: mint sandbox sessions and run payment scenarios against the paycross_flutter plugin."
# Independent of the plugin's version on purpose -- this is the demo app's
# own release train, and both release scripts pass --build-name/--build-number
# derived from the demo-vX.Y.Z tag. Without a version: line here every build
# is 1.0.0+1, which App Store Connect rejects on the second upload.
version: 0.1.0+1
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  paycross_flutter:
    # When depending on this package from a real application you should use:
    #   paycross_flutter: ^x.y.z
    # See https://dart.dev/tools/pub/dependencies#version-constraints
    # The example app is bundled with the plugin so we use a path dependency on
    # the parent directory to use the current plugin's version.
    path: ../

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Reads CFBundleShortVersionString/CFBundleVersion on iOS and
  # versionName/versionCode on Android -- the same two fields the release
  # scripts set from the tag, so the version Settings shows and the version
  # the store sees cannot drift apart.
  package_info_plus: ^10.2.1

  # Injectable Client, and package:http/testing.dart's MockClient is what
  # every minter test runs against -- so the whole module is covered with no
  # network and no platform channel.
  http: ^1.6.0

  # Keychain on iOS, EncryptedSharedPreferences on Android. Nothing else in
  # this app may hold a credential.
  flutter_secure_storage: ^11.0.0

  # History only. It holds ids, outcomes and versions -- never a token, and
  # never a credential, which is why this is not the secure store.
  shared_preferences: ^2.5.5

  # Its floor is exactly this plugin's -- Flutter >=3.44.0, Dart ^3.12.0 --
  # and it implements the FlutterSceneLifeCycleDelegate callbacks this
  # Runner needs, because the Runner uses FlutterSceneDelegate.
  app_links: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
